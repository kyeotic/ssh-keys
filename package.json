{
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-keys.js && wrangler dev",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "deploy:infra": "./infra/deploy"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
